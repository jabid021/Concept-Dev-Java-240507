logging:
  level:
    formation:
      conceptdev:
        formationSpringboot:
          controllers: debug
          services:
            HelloService: trace
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quest_jsp
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root123@
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        '[format_sql]': true
    show-sql: true
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
      
server:
  servlet:
    context-path: /demo      